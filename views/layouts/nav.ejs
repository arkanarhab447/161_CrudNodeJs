<% if (user) { %> 
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid mx-auto">
            <a class="navbar-brand" href="/">
                <img src="/images/ugm.png" alt="Logo" style="height: 50px;">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- Navbar Menu di tengah -->
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/todo-view">Todo</a>
                    </li>
                </ul>
                <!-- Tombol Logout di paling kanan dengan logo -->
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">
                            <!-- Gambar logo logout di samping teks -->
                            <img src="/images/logout.png" alt="Logout Logo" style="height: 30px; margin-right: 5px;">
                            Logout
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
<% } %>

<!-- Navbar tidak ditampilkan pada halaman login -->
<% if (!user) { %> 
    <!-- Halaman login, tidak menampilkan navbar -->
    <!-- Navbar bisa dibiarkan kosong atau hanya menampilkan logo tanpa link -->
<% } %>
